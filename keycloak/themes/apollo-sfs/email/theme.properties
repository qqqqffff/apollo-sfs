# Apollo SFS email theme. Extends the base Keycloak email theme; we only override
# the HTML wrapper (html/template.ftl), so every inherited email (password reset,
# verify email, etc.) renders inside the Apollo SFS frame automatically.
parent=keycloak
